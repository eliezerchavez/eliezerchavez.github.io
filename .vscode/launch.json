{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "request": "launch",
      "name": "Edge: localhost (WSL server)",
      "url": "http://localhost:8000/index.html",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "Chrome: localhost (WSL server)",
      "url": "http://localhost:8000/index.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
